[project]
name = "agentic-ai-service"
version = "0.1.0"
description = "Python AI, Data, and MCP microservice"
authors = [{ name = "Shailesh Patel", email = "shailesh17@gmail.com" }]
readme = "README.md"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
