image: node:22

stages:
  - build
  - test

cache:
  key:
    files:
      - pnpm-lock.yaml
  paths:
    - .pnpm-store

before_script:
  - apt-get update -qq && apt-get install -y -qq golang-go python3
  - corepack enable
  - corepack prepare pnpm@latest --activate
  - pnpm config set store-dir .pnpm-store
  - pnpm install --frozen-lockfile

build:
  stage: build
  script:
    - pnpm run build
  artifacts:
    paths:
      - apps/*/dist/
      - packages/*/dist/

test:
  stage: test
  script:
    - pnpm test
